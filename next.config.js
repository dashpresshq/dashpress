/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  forceSwcTransforms: true,
  experimental: {
    forceSwcTransforms: true,
  },
};

module.exports = nextConfig;
