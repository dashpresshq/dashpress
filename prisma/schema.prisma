datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:password@localhost:5432/kademiks?schema=public"
}

generator custom_generator {
  provider = "cd ./generator && ts-node bin.ts"
}
