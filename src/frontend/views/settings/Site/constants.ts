import { MAKE_CRUD_CONFIG } from "frontend/lib/makeCrudConfig";

export const SITE_SETTINGS_CRUD_CONFIG = MAKE_CRUD_CONFIG({
  path: "N/A",
  plural: "Site Settings",
  singular: "Site Settings",
});
